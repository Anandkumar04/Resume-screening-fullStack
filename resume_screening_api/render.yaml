services:
  - type: web
    name: resume-screening-ai
    env: python
    plan: free
    runtime: python
    buildCommand: |
      echo "🔧 Installing dependencies with Python 3.11..."
      pip install --upgrade pip
      pip install -r requirements.txt
      echo "📥 Verifying PDF processing libraries..."
      python -c "import PyPDF2; print('✅ PyPDF2 ready')"
      python -c "import pdfplumber; print('✅ pdfplumber ready')"
      echo "✅ Build completed successfully!"
    startCommand: "gunicorn --bind 0.0.0.0:$PORT wsgi:app"
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PYTHONPATH
        value: /opt/render/project/src
      - key: MAX_CONTENT_LENGTH
        value: "16777216"
      - key: PYTHON_VERSION
        value: "3.11.9"
    autoDeploy: true
